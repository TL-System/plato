[project]
name = "smog"
version = "0.1.0"
description = "SMoG - Unsupervised Visual Representation Learning by Synchronous Momentum Grouping"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "scikit-learn",
]
