[project]
name = "gradient-leakage-attacks"
version = "0.1.0"
description = "Gradient Leakage in Production Federated Learning"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lpips",
    "matplotlib",
]
