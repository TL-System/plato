[clients]

# The total number of clients
total_clients = 10 

# The number of clients selected in each round
per_round = 4

# Should the clients compute test accuracy locally?
do_test = true

# Percentage of data used for local testing
test_partition = 0.2

[data]

# Number of samples in each partition
partition_size = 600

# IID, biased, or sharded?
divider = iid

# Uniform or normal distribution for labels across clients?
label_distribution = uniform

[training]

# The maximum number of training rounds
rounds = 10000

target_accuracy = 0.95

task = train

# Number of epoches for local training in each communication round
epochs = 5

batch_size = 10

dataset = MNIST
data_path = ./data

model = mnist_cnn

optimizer = SGD
learning_rate = 0.01
momentum = 0.5

[server]

# Aggregation algorithm
type = fedavg

address = localhost
port = 8000

[cross_silo]

# The total number of silos (edge servers)
total_silos = 2

# The number of local aggregation rounds on edge servers before sending aggreagted weights to the central server
rounds = 2
